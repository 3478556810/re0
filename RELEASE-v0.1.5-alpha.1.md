# Rescene v0.1.5-alpha.1

## 项目与工作目录

- 修复工作目录菜单的 **Open folder...**：现在打开 Windows 原生文件夹选择器，不再错误地展示当前仓库的子文件夹。
- 创建项目时，文件夹选择失败会显示明确错误，不再出现“点了没反应”。
- 创建项目弹窗新增 **最近选择项目** 下拉栏，可直接复用近期项目作为源文件夹。

## 提供方专项优化

- 新增 **B.AI**：DeepSeek V4 Flash、DeepSeek V4 Flash Vision Exp、Tencent Hy3；使用用户自己的 B.AI API Key。
- B.AI 在大陆网络下优先通过 DoH 解析 `api.b.ai`，避免受系统 DNS 异常影响；同时复用用户已启用的标准代理或本机 Clash 常用端口。
- B.AI 的普通请求、Responses API 与流式对话统一使用同一套网络适配，失败仍交给既有路由与熔断链处理。
- 新增 OpenRouter **Ox Alpha** 限时免费模型入口；限时活动、额度与最终计费以各提供方平台显示为准。

## 站点一键发布

- 新增“站点”入口：自动识别当前项目的 `dist`、`build`、`out` 或根目录中的静态构建产物，并预选可发布目录。
- 无需用户准备 ZIP；选择构建产物后即可一键发布或更新到用户自己的 Netlify 站点，并保留已发布链接。
- 发布前必须粘贴用户自己的 Netlify Personal Access Token 并勾选公开确认；令牌仅用于本次请求、不落盘。
- 对无 `index.html`、越界目录、超过 100MB 的构建产物和 Netlify 子域名冲突提供明确反馈；冲突时填入可编辑建议名称，需重新确认后才会发布。

## 验证范围

- B.AI 目录解析、DoH 响应解析、代理优先级及直连拨号均有自动化测试。
- 未包含真实 B.AI API 调用：该测试仅在发布环境配置了用户自己的 `BAI_API_KEY` 时执行。
